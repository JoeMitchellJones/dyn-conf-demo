version: 2.1

setup: true

orbs:
  config-splitting: circle-makotom-orbs/config-splitting@0.0.8

workflows:
  setup:
    jobs:
      - config-splitting/run-all-modular-configs:
          config-list-file: config-files.txt
          shared-config-file: .circleci/shared_config.yml
